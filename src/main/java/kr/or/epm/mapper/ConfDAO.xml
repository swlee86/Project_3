<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.epm.DAO.ConfDAO">

	<!-- 회의 등록하기 -->
	<insert id="insertConf">
		insert into conf(conf_no, emp_no, conf_start, 
						 conf_end, conf_item, conf_content, 
						 conf_record, conf_date)
		valuse(conf_seq.nextval, #{emp_no}, #{conf_start}, 
			   #{conf_end}, #{conf_item}, #{conf_content}, 
			   #{conf_record}, to_char(sysdate, 'yyyy-mm-dd'))
	</insert>
	
	<!-- 회의 수정하기 -->
	<update id="updateConf">
		update conf
		set emp_no = #{emp_no},
			conf_start = #{conf_start},
			conf_end = #{conf_end},
			conf_item = #{conf_item},
			conf_content = #{conf_content},
			conf_record = #{conf_record}
		where conf_no = #{conf_no}
	</update>
	
	<!-- 회의 삭제하기 -->
	<delete id="deleteConf">
		delete from conf
		where conf_no = #{conf_no}
	</delete>
	
	<!-- 회의 조회하기 -->
	<select id="selectConf" resultType="kr.or.epm.VO.Conf">
		select conf_no, emp_name, conf_item, conf_record
		from conf
		order by conf_start desc
	</select>
	
</mapper>